from enum import Enum


class CalcCaption(Enum):
    CLEAR = 'C',
    INVERT_SIGN = '+/-',
    PERCENT = '%',
    DIVIDE = '/',
    MULTIPLY = '*',
    SUBTRACT = '-',
    ADD = '+',
    DECIMAL = '.',
    EQUALS = '='
